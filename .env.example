# Browser requests stay same-origin and are forwarded by Vite in development.
VITE_API_BASE_URL=/api

# Use http://web when running through fraudebot-docker.
VITE_API_PROXY_TARGET=http://localhost:9000
